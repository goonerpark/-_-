### DESIGN.md

## 1. 데이터 분석

### 1.1 전체 구조 분석

metadata.json은 크게 아래와 같은 구조로 구성되어 있었습니다.
- project : 프로젝트 이름, 좌표 단위
- disciplines : 사용되는 공종 목록
- drawings : 도면 데이터(계층 구조의 핵심)

drawings는 "도면 번호"를 key로 가지며, 각 drawing은 다음 요소를 포함합니다.
- id, name, image
- parent : 상위 도면 번호
- position : 상위 도면 위에서 도면이 차지하는 영역 
- disciplines : 도면에 속한 공종별 데이터   
---

### 1.2 최적의 구조 구상

drawings의 구조를 분석했을 때, UI 내에서 사용자가 자연스럽게 탐색하는 흐름은 [ 프로젝트 -> 도면 -> 공종 -> Region -> 리비전 ]과 같은 구조가 가장 직관적이라고 판단했습니다.
따라서 metadata.json의 구조를 UI에 그대로 노출하는 것이 아닌 사용자가 실제 탐색하는 방식인 폴더 탐색 방식을 반영한 트리구조로 구성하게되었습니다
---

### 1.3 특수 구조 처리

metadata.json에는 일반적인 도면과 다른 특수 구조가 존재합니다.

   #### 1. 구조 공종은 region을 가지는 경우가 있음
   예: 101동 구조
   - Region A → REV1A, REV2A
   - Region B → REV1B, REV2B

   위와 같은 구조인 경우 공종 아래에 Region 노드를 추가하고, Region 노드 아래에 Revision 노드를 배치했습니다.
   또한 구조 공종 자체 이미지가 존재하는 경우(BASE)를 별도 노드로 제공했습니다.

   ---

   #### 2. Revision이 공종이 아닌 Revision 자체에 imageTransform을 가지는 경우
   예: 주민공동시설 건축

   이 경우 공종 레벨에는 imageTransform이 없고
   Revision마다 독립적인 imageTransform과 polygon을 가지고 있었습니다.
   따라서 UI에서는 Revision이 독립적인 뷰 단위가 되도록 구성했습니다.

   ---

   ### 2.4 더 나은 데이터 표현 가능성
   현재 metadata.json만으로도 도면을 탐색하는데는 큰 어려움을 느끼지않았습니다.
   하지만, 실제 현장에선 매 순간 변경사항이 발생하고, 공종 간 간섭을 확인해야 할 상황이 자주 발생하는 만큼 추가적인 요소가 있었으면 좋겠다고 생각하게 되었습니다.
   제가 생각한 추가 요소는 다음과 같습니다.

   - 최신 리비전 여부 표시
      - 각 공종별로 어떤 리비전이 최신인지 isLatest와 같은 필드로 명시
   - 리비전 간 연결 관계
      - REV3의 이전 버전이 REV2가 맞는지 혼동하는 경우가 생길 수 있기에, Revision간 흐름을 데이터로 연결
   - 기준 도면 관계 명확화
      - 오버레이 기능을 구현하면서 relativeTo가 파일명 문자열로 되어있어 관리가 어려울 것 같다는 생각을 하게 되었습니다. 기준 도면이 id기반으로 연결되어 있으면, 도면명이 바뀌어도 안전하게 매칭할 수 있어 로직이 더욱 안정적일 것 같습니다.

---

## 2. 접근 방식 (해결 순서)

과제는 다음 순서로 진행했습니다.

1) metadata.json 구조 분석 및 트리 구조 설계
2) Tree UI 구현 (recursive component)
3) 선택한 노드에 대한 viewer 구성
4) Context 인식(브레드크럼 + 선택 정보) 구현
5) Revision 정보 표시 UI 구현
6) 공종 간 간섭 확인을 위한 오버레이 기능 추가

위 순서대로 필수 요구사항을 먼저 만족한 뒤, 사용자 시나리오에 직접 연결되는 기능을 구현하는 방식으로 접근했습니다.
---

## 3. UI 설계 결정

   ### 3.1 레이아웃 선택

   좌측: 트리 탐색
   우측: 도면 뷰어 + 상세 정보

   이 레이아웃을 선택한 이유는 다음과 같습니다.

   - 도면 탐색은 "계층 구조" 기반으로 이루어지므로 트리가 가장 직관적
   - 도면 확인은 큰 이미지가 필요하므로 우측을 뷰어에 집중 배치
   - 사용자가 왼쪽에서 선택 → 오른쪽에서 확인하는 흐름이 자연스럽고 학습 비용이 낮음

   ---

   ### 3.2 컨텍스트 인식 UI

   트리 탐색 UI는 깊이가 깊어질수록 사용자가 현재 위치를 잃기 쉽습니다.

   이를 해결하기 위해 우측 상단에 선택한 노드의 path를 기반으로
   브레드크럼을 항상 표시했습니다.

   예:
   샘플 아파트 단지 › 01. 101동 지상1층 평면도 › 구조 › Region A › REV2A

   이 방식을 통해 사용자가 현재 어떤 도면을 보고있는지 확실히 인지할 수 있도록 하였습니다.

   ---

   ### 3.3 오버레이 기능 설계 (공종 간 간섭 확인)

   사용자 시나리오 중  
   "설비 배관이 건축 벽체 변경과 충돌하지 않는지 확인"은
   단순히 도면을 보는 것만으로 해결되지 않습니다.

   따라서 같은 drawingId 내에서 다른 공종/리비전 이미지를 선택하여
   겹쳐볼 수 있는 오버레이 모드를 제공했습니다.

   - 투명도 조절
   - 미세 위치 이동(dx/dy)
   - 스케일 조정
   - 회전 각도 조정

   오버레이 기능을 통해 사용자는 공종 간 간섭을 확인하고, 투명도를 조절하여 보다 쉽게 도면을 비교 할 수 있습니다.

---

## 4. 기술 선택

   ### 4.1 Vue 3 + TypeScript

   이 프로젝트는 도면 데이터가 프로젝트 -도면 - 공종 - 리전 -리비전 처럼 단계가 깊게 이어지는 구조였습니다. 
   따라서 같은 UI를 반복해서 그릴 수 있는 재귀 형태의 트리 컴포넌트가 필요하다고 생각했고, Vue는 이런 구조를 비교적 깔끔하게 구현할 수 있다고 판단했습니다.
   또한 metadata.json은 도면마다 구조가 조금씩 다르고, 어떤 필드는 있는 경우도 있고 없는 경우도 많았습니다.
   TypeScript를 사용하면 이런 데이터를 다룰 때 실수로 잘못된 값을 접근하는 문제를 줄일 수 있어, 구현 과정에서 안정성을 확보하는 데 도움이 되었습니다.
   ---

   ### 4.2 스타일링 방식

   Tailwind/SCSS 등을 사용하지 않고
   Vanilla CSS로 구현했습니다.

---

## 5. 어려웠던 점 및 개선 방안

   ### 5.1 어려웠던 점
   - metadata.json의 공종/리비전 구조가 도면마다 달라 일반화된 트리 빌드 로직이 필요했습니다.

   - 자동정렬 기능을 구현하는 부분에서 imageTransform의 의미가 정확히 어떤 기준 좌표인지 데이터만으로 100% 확정하기 어려워 구현에 어려움이 있었습니다.
   ---

   ### 5.2 개선 방안

   - relativeTo 기반 매핑을 강화하여 base 도면과 overlay 도면의 기준 이미지 관계를 더 정확하게 계산하여 오버레이 정렬 정확도를 개선하겠습니다.

   - 선택한 REV의 이전 REV를 자동 선택하게하여 리비전 비교 기능을 더욱 자세히 하고싶습니다.

   - 도면 이름 검색, 공종 필터, 최신 리비전만 보기 옵션과 같은 검색/필터 UI를 추가하여 사용자가 원하는 자료를 찾기 더욱 수월하게 구성하고 싶습니다.
   ---